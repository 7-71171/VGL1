<Page x:Class="VGL1.Menus.Examples"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:VGL1.Menus"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Examples">
    <Page.DataContext>
        <local:ExamplesViewModel />
    </Page.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="20" />
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="プログラムIDを入力してください:" Margin="50,17,0,0" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" Height="20" Margin="50,0,0,0">
            <TextBox Text="{Binding AppIdInput}" Width="300" />
            <Button Content="実行" Command="{Binding ExecuteCommand}" Width="50" Margin="15,0,0,0" BorderThickness="0" />
        </StackPanel>
        <TextBlock Name="ErrorTextBlock" Grid.Row="2" Text="正しい値を入力してください" Foreground="Red" Margin="50,17,0,0" />
        <DataGrid Grid.Row="3" ItemsSource="{Binding AppItems}" Margin="50,0,0,0" Width="500" HorizontalAlignment="Left"
                  HeadersVisibility="Column"
                  GridLinesVisibility="None"
                  BorderThickness="0"
                  Background="Transparent"
                  AlternatingRowBackground="Transparent"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeRows="False"
                  CanUserResizeColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding AppId}" Width="50" />
                <DataGridTextColumn Header="プログラム名称" Binding="{Binding AppName}" Width="150" />
                <DataGridTextColumn Header="説明" Binding="{Binding AppDescription}" Width="300" />
            </DataGrid.Columns>
        </DataGrid>


    </Grid>
</Page>
